# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration
# 支持的类型: sqlite (sqlite3), sqljs (sql.js), mysql, postgres
DB_TYPE=sqlite
SQLITE_STORAGE=./database.sqlite

# CORS Configuration
CORS_ORIGIN=*
