import 'package:flutter/material.dart';

class BasicFormInfo extends StatelessWidget {
  const BasicFormInfo({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text('组件内容');
  }
}
